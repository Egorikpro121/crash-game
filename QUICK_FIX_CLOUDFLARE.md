# Быстрое исправление: Cloudflare Pages вместо Worker

## ❌ Что не так
Вы создали **Cloudflare Worker**, но для фронтенда нужен **Cloudflare Pages**.

## ✅ Что делать

### Вариант 1: Создать Pages проект (Рекомендуется)

1. **Откройте Cloudflare Dashboard**
2. **Слева выберите "Pages"** (не Workers!)
3. **Нажмите "Create a project"**
4. **Выберите "Connect to Git"**
5. **Выберите репозиторий:** `Egorikpro121/crash-game`
6. **Нажмите "Begin setup"**

### Настройки Build:

```
Framework preset: Vite (или None)
Build command: cd frontend && npm install && npm run build
Build output directory: frontend/dist
Root directory: / (оставьте пустым)
```

7. **Нажмите "Save and Deploy"**

### Вариант 2: Исправить Worker (если очень нужно)

Если вы хотите использовать Worker для раздачи статики (не рекомендуется):

1. В настройках Worker измените:
   - **Build command:** `cd frontend && npm install && npm run build`
   - **Deploy command:** `npx wrangler pages deploy frontend/dist`
   - **Root directory:** `/frontend/dist`

Но это сложнее и не рекомендуется для статических сайтов.

---

## 🎯 Рекомендация

**Используйте Cloudflare Pages** - это правильный инструмент для фронтенда Telegram Mini App.

Worker оставьте для backend функций (если понадобятся в будущем).
