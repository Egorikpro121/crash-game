# Настройка через Cloudflare - Пошаговая инструкция

## 🎯 Вы зарегистрировались в Cloudflare через Google - что дальше?

У вас есть **2 варианта**:

### ✅ Вариант 1: Бесплатный домен через Cloudflare Pages (Рекомендуется)

Не нужно покупать домен! Cloudflare Pages даст вам бесплатный домен типа `your-project.pages.dev`

### ✅ Вариант 2: Купить домен через Cloudflare

Если хотите свой домен (например, `crashgame.com`)

---

## 🚀 Вариант 1: Cloudflare Pages (Бесплатно, 5 минут)

### Шаг 1: Перейдите в Cloudflare Pages

1. В левом меню найдите раздел **"Build"**
2. Нажмите на **"Pages"** (или перейдите напрямую: https://dash.cloudflare.com/pages)

### Шаг 2: Подключите GitHub

1. Нажмите кнопку **"Create a project"**
2. Выберите **"Connect to Git"**
3. Выберите **GitHub**
4. Разрешите доступ Cloudflare к вашему GitHub аккаунту
5. Выберите репозиторий с вашим проектом

### Шаг 3: Настройте проект

**Build settings:**
- **Framework preset**: Vite (или оставьте пустым)
- **Build command**: `cd frontend && npm run build`
- **Build output directory**: `frontend/dist`
- **Root directory**: `/` (оставьте пустым)

**Environment variables:**
- `VITE_API_URL` = `https://your-backend-url.com` (пока можно оставить пустым)

### Шаг 4: Сохраните и задеплойте

1. Нажмите **"Save and Deploy"**
2. Подождите 2-3 минуты пока соберется проект
3. **Готово!** Вы получите домен типа: `your-project-name.pages.dev`

### Шаг 5: Настройте Telegram бота

Теперь у вас есть домен! Настройте бота:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте:
   ```
   /setmenubutton
   ```
3. Выберите вашего бота
4. Введите текст кнопки: `🎮 Играть`
5. Введите URL: `https://your-project-name.pages.dev`
6. Готово!

---

## 💰 Вариант 2: Купить домен через Cloudflare

### Шаг 1: Купить домен

1. На странице Domain Management нажмите **"Buy a domain"**
2. Введите желаемое имя домена (например: `crashgame`)
3. Выберите зону (.com, .ru, .dev и т.д.)
4. Добавьте в корзину и оплатите

**Цены:**
- .com: ~$10-12/год
- .ru: ~$5-8/год
- .dev: ~$15-20/год

### Шаг 2: Настроить DNS для Pages

После покупки домена:

1. Перейдите в **Pages** → ваш проект
2. Откройте вкладку **"Custom domains"**
3. Нажмите **"Set up a custom domain"**
4. Введите ваш домен (например: `crashgame.com`)
5. Cloudflare автоматически настроит DNS записи
6. SSL сертификат настроится автоматически (может занять 5-10 минут)

### Шаг 3: Настроить Telegram бота

Используйте ваш новый домен:
```
/setmenubutton
[Выберите бота]
[Текст: 🎮 Играть]
[URL: https://crashgame.com]
```

---

## 📋 Что делать прямо сейчас (быстрый путь)

### 1. Создайте проект в Cloudflare Pages (2 минуты)

1. В левом меню → **"Build"** → **"Pages"**
2. **"Create a project"** → **"Connect to Git"** → **GitHub**
3. Выберите ваш репозиторий
4. Настройки:
   ```
   Build command: cd frontend && npm run build
   Output directory: frontend/dist
   ```
5. **"Save and Deploy"**

### 2. Получите домен (автоматически)

После деплоя вы увидите домен типа:
```
https://your-project-name.pages.dev
```

### 3. Настройте бота (1 минута)

Откройте [@BotFather](https://t.me/BotFather):
```
/setmenubutton
[Выберите бота]
[Текст: 🎮 Играть]
[URL: https://your-project-name.pages.dev]
```

### 4. Проверьте

1. Откройте вашего бота в Telegram
2. Нажмите кнопку меню
3. Mini App должна открыться! 🎉

---

## 🔧 Настройка Backend (для полной работы)

### Вариант A: Railway (Рекомендуется)

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. **New Project** → **Deploy from GitHub**
4. Выберите ваш репозиторий
5. Добавьте переменные окружения:
   ```
   SECRET_KEY=your-secret-key
   DATABASE_URL=postgresql://...
   TELEGRAM_BOT_TOKEN=your-bot-token
   FRONTEND_URL=https://your-project-name.pages.dev
   ```
6. Railway создаст домен автоматически (например: `your-project.railway.app`)

### Обновите Frontend

В Cloudflare Pages → ваш проект → Settings → Environment variables:
```
VITE_API_URL=https://your-project.railway.app
```

Пересоберите проект (Deployments → Retry deployment)

---

## ⚠️ Важные моменты

1. **HTTPS обязателен** - Cloudflare Pages автоматически дает HTTPS
2. **Домен должен быть доступен** - проверьте в браузере перед настройкой бота
3. **SSL может занять время** - при использовании своего домена подождите 5-10 минут

---

## 🆘 Проблемы?

### Pages не собирается
- Проверьте, что в репозитории есть папка `frontend`
- Убедитесь, что `package.json` есть в `frontend/`
- Проверьте логи сборки в Cloudflare

### Домен не работает
- Подождите 2-3 минуты после деплоя
- Проверьте в браузере напрямую
- Убедитесь, что URL начинается с `https://`

### Mini App не открывается
- Проверьте URL в BotFather (должен быть `https://`)
- Убедитесь, что сайт открывается в браузере
- Проверьте, что нет ошибок в консоли браузера

---

## 📞 Следующие шаги

После настройки Pages:

1. ✅ Получите домен от Cloudflare Pages
2. ✅ Настройте бота через BotFather
3. ✅ Задеплойте backend (Railway/Render)
4. ✅ Обновите переменные окружения
5. ✅ Протестируйте Mini App

**Готово!** 🎉
