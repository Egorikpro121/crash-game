# 📦 Загрузка проекта в GitHub

## Нужно ли создавать новый репозиторий?

**Да!** Cloudflare Pages работает только с GitHub репозиториями. Нужно загрузить ваш проект в GitHub.

---

## 🚀 Пошаговая инструкция

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** вверху справа → **"New repository"**
4. Заполните:
   - **Repository name**: `crash-game` (или любое другое имя)
   - **Description**: "Telegram Mini App Crash Game"
   - **Visibility**: Public (или Private - как хотите)
   - **НЕ** ставьте галочки на "Add README", "Add .gitignore", "Choose a license"
5. Нажмите **"Create repository"**

### Шаг 2: Загрузите проект в GitHub

Откройте терминал в папке проекта и выполните:

```bash
cd /home/egor/Музыка/obuchenie_crypto

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Crash Game Mini App"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/crash-game.git

# Загрузите код
git branch -M main
git push -u origin main
```

### Шаг 3: Если git уже настроен

Если у вас уже есть git репозиторий, просто:

```bash
# Проверьте текущий remote
git remote -v

# Если нужно изменить URL
git remote set-url origin https://github.com/YOUR_USERNAME/crash-game.git

# Загрузите код
git push -u origin main
```

---

## 🔧 Быстрый способ через GitHub CLI (если установлен)

```bash
# Установите GitHub CLI (если нет)
# Ubuntu/Debian:
sudo apt install gh

# Войдите
gh auth login

# Создайте репозиторий и загрузите код
cd /home/egor/Музыка/obuchenie_crypto
gh repo create crash-game --public --source=. --remote=origin --push
```

---

## 📋 Что будет загружено

В репозиторий попадет:
- ✅ Весь код проекта (backend + frontend)
- ✅ Конфигурационные файлы
- ✅ Документация
- ❌ НЕ попадут: `.env` файлы (они в `.gitignore`)
- ❌ НЕ попадут: `node_modules` (они в `.gitignore`)
- ❌ НЕ попадут: `__pycache__` (они в `.gitignore`)

---

## ⚠️ Важно перед загрузкой

### 1. Проверьте .gitignore

Убедитесь, что в `.gitignore` есть:
```
.env
.env.local
node_modules/
__pycache__/
*.pyc
*.db
*.sqlite
dist/
build/
```

### 2. НЕ загружайте секретные данные

Убедитесь, что в коде нет:
- Токенов ботов
- Секретных ключей
- Паролей от баз данных

Все это должно быть в `.env` файлах, которые не попадут в репозиторий.

---

## ✅ После загрузки

1. Проверьте на GitHub, что все файлы загружены
2. Перейдите в Cloudflare Pages
3. Подключите этот репозиторий
4. Задеплойте проект

---

## 🆘 Проблемы?

### Ошибка "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/crash-game.git
```

### Ошибка авторизации
Используйте Personal Access Token вместо пароля:
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token
3. Используйте токен как пароль при `git push`

### Большой размер репозитория
Если проект большой, добавьте в `.gitignore`:
```
frontend/node_modules/
frontend/dist/
*.db
*.sqlite
```
